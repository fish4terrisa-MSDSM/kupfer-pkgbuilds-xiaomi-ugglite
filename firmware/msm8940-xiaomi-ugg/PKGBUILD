_mode=cross
pkgname=firmware-msm8940-xiaomi-ugg
pkgver=0.1
pkgrel=2
_arches=specific
arch=(aarch64)
license=(GPL)
depends=(linux-firmware-qcom)
source=("adreno-quirks.sh")
sha256sum=('SKIP')
options=(!strip)

build() {
    cd "$srcdir"
    echo "placeholder"
}

package() {
    mkdir -p $pkgdir
    install -Dm644 "$srcdir"/adreno-quirks.sh "$pkgdir"/etc/profile.d/adreno-quirks.sh
}
